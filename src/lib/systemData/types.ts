export interface TrialDataCounts {
  dutyPlanCount: number;
  examInvigilationPlanCount: number;
  assignmentListCount: number;
  payrollRecordCount: number;
  closedPeriodCount: number;
}

export interface SystemDataApiError {
  code: string;
  message: string;
}
